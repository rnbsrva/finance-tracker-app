type Category implements BaseEntity {
    id: ID!
    createdDate: LocalDateTime
    lastModifiedDate: LocalDateTime
    name: String
    user: User
}

input CategoryDTO{
    name:String
    userId: ID
}